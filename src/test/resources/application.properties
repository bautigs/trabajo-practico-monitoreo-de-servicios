# Valores dummy para los tests: el application.yml de main referencia
# ${DB_URL}, ${KEYCLOAK_ISSUER_URI}, etc. que en el JVM de test no existen
# (no se carga el .env). Con esto los placeholders resuelven y el contexto
# de los slice tests (@WebMvcTest) levanta.
DB_URL=jdbc:h2:mem:testdb
DB_USER=sa
DB_PASSWORD=
KEYCLOAK_ISSUER_URI=http://localhost:8081/realms/monitoreo-servicios
KEYCLOAK_SERVER_URL=http://localhost:8081
KEYCLOAK_BACKEND_CLIENT_ID=monitoreo-backend
KEYCLOAK_BACKEND_SECRET=test-secret
KEYCLOAK_BACKEND_USERNAME=svc-backend
KEYCLOAK_BACKEND_PASSWORD=test-pass
